# Flyway

There are three Flyway commands that you'll use most often:

- `task db:flyway:clean` to remove all objects from the database.
- `task db:flyway:info` to show the status of the database.
- `task db:flyway:migrate` to apply migrations.
